# Antiplagiarism - preliminary implementation

Re-create my python virtual environment using

```pip install -r requirements.txt```

Then, run the command

```python processor.py --filepath /absolute/path/to/commedia.txt```
