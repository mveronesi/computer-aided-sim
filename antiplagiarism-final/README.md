First of all, recreate my environment using the command 

```
pip install -r requirements.txt
```

then use

```
python processor.py --filepath /path/to/commedia.txt
```

To see other options use

```
python processor.py -h
```
